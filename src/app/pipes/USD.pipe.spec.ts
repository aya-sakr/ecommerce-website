/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { USDPipe } from './USD.pipe';

describe('Pipe: USDe', () => {
  it('create an instance', () => {
    let pipe = new USDPipe();
    expect(pipe).toBeTruthy();
  });
});
